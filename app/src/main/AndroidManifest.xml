<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simplewen.win0.wd">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/wd"
        android:label="@string/app_name"
        android:roundIcon="@drawable/wd"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WDMain"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".brow"
            android:label="我的借阅"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".history"
            android:label="我的借阅历史"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".search_login"
            android:label="图书搜索"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".getNotice"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".libraryweb.libraryweb"></activity>
    </application>

</manifest>